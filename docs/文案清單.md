# PN Anywhere — 全部顯示文字

每一段都標了檔案:行號。你直接在這份上面改，或跟我說「第幾號改成什麼」。

---

## 畫面 1 ｜ 第一次啟動的引導視窗

只在沒有設定檔時出現一次。`ui/welcome.py`

| # | 位置 | 目前的字 |
|---|---|---|
| 1-1 | 視窗標題 | 歡迎使用 PN Anywhere |
| 1-2 | 大標 | **PN Anywhere 已經在執行了** |
| 1-3 | 副標 | 它住在螢幕右上角的選單列，長得像上面那顆晶片。<br>這個程式沒有主視窗，所以 Dock 上不會有它 —— 這是正常的。<br>（Windows 版：「右下角的工作列」「工作列上不會有它」） |
| 1-4 | 步驟①標題 | 開啟「輔助使用」權限 　*(只有 macOS 且尚未授權才出現)* |
| 1-5 | 步驟①內文 | 熱鍵要這個權限才能用。按右邊直接開設定頁，把清單裡的本程式打勾。 |
| 1-6 | 步驟①按鈕 | 去開啟 |
| 1-7 | 步驟②標題 | 匯入你的報價單 / 庫存表 |
| 1-8 | 步驟②內文 | Excel 或 CSV 都可以，最多三個檔案。程式會用料號把它們串起來。 |
| 1-9 | 步驟②按鈕 | 匯入 |
| 1-10 | 步驟③標題 | 按 ⌥⇧Space 查詢 |
| 1-11 | 步驟②內文 | 也就是 option + shift + 空白鍵。在任何軟體裡都能按。**先把料號複製起來再按，它會自動填好、直接把答案查出來**；沒複製也行，按了打字就開始找。查完按 esc 關掉。 |
| 1-12 | 主按鈕 | 知道了 |
| 1-13 | 頁尾 | 這個畫面只會出現這一次。之後有問題就點螢幕右上角的選單列的圖示 →「診斷資訊…」。 |

---

## 畫面 2 ｜ 選單列圖示的下拉選單

`main.py:373-401`

| # | 目前的字 | 備註 |
|---|---|---|
| 2-1 | 查詢　　　　　⌥⇧Space | |
| 2-2 | ⚠︎  開啟輔助使用權限… | 平常隱藏 |
| 2-3 | ⚠︎  熱鍵沒掛上… | 平常隱藏 |
| 2-4 | ⚠︎  載入有問題… | 平常隱藏 |
| 2-5 | 匯入 Excel 資料… | |
| 2-6 | 重新載入 | |
| 2-7 | 診斷資訊… | |
| 2-8 | 結束 | |

**滑鼠停在圖示上的提示字**（`main.py:162,165,177,185`）

| # | 目前的字 |
|---|---|
| 2-9 | PN Anywhere |
| 2-10 | PN Anywhere —— 載入中… |
| 2-11 | PN Anywhere　·　12,345 筆資料 |
| 2-12 | PN Anywhere　·　尚未匯入資料 |

**系統通知**（`main.py:181,339`）

| # | 目前的字 |
|---|---|
| 2-13 | 已重新載入 12,345 筆資料 |
| 2-14 | 權限已開啟，現在可以按 ⌥⇧Space 查詢了 |

---

## 畫面 3 ｜ 熱鍵查詢視窗

`ui/popup.py`、`ui/result_view.py`

### 3A 空白狀態

| # | 目前的字 |
|---|---|
| 3-1 | 搜尋框提示字：輸入料號，邊打邊找 |
| 3-1b | **（沒有文字，但是行為）按熱鍵時如果剪貼簿裡是單行、64 字以內的內容，會自動填進搜尋框並且直接查。查不到就顯示 3-8 的「查無」，直接打字覆蓋掉即可。** |
| 3-2 | 打字就直接找　·　↑↓ 選　·　return 看完整資料 |
| 3-3 | 資料載入中… |
| 3-4 | **資料載入失敗** ／ 請重新匯入 |
| 3-5 | **尚未匯入資料** ／ 點選單列圖示 → 匯入 Excel 資料 |

### 3B 結果清單

| # | 目前的字 |
|---|---|
| 3-6 | 每列右邊摘要：`$12.5　·　庫存 300　·　3 筆`（最後那個「N 筆」只有料號重複時出現） |
| 3-7 | 底部：↑↓ 選　·　return 看完整資料　·　esc 關閉 |
| 3-8 | **查無「XXXX」** ／ 你匯入的資料裡沒有相近的料號 |
| 3-9 | **查詢時出錯** ／（錯誤訊息） |

### 3C 單筆詳細

| # | 目前的字 |
|---|---|
| 3-10 | 料號右邊小標籤：`2 / 3 筆`（只有重複時） |
| 3-11 | 你輸入的是「XXXX」，忽略符號差異後對到上面這筆 |
| 3-12 | 沒有值的欄位顯示：— |
| 3-13 | 這幾個檔案查不到這筆料號：A.xlsx、B.xlsx |
| 3-14 | 底部：第 2 / 6 筆　·　↑↓ 換一筆　·　← → 看這個料號的其他列　·　esc 回清單 |

---

## 畫面 4 ｜ 匯入 Price Book

`ui/import_dialog.py`

### 4A 標題與步驟

| # | 目前的字 |
|---|---|
| 4-1 | 視窗標題：匯入 Excel 資料 |
| 4-2 | 大標：PN Anywhere |
| 4-3 | 副標：最多 3 個檔案，用彼此共用的料號欄位串成同一張查詢卡 |
| 4-4 | 步驟 1：放進 Excel 檔案　／　提示：最多 3 個，可以只放一個 |
| 4-5 | 步驟 2：挑要顯示哪些欄位　／　提示：把想看的拖到右邊 |
| 4-6 | 步驟 2 說明：欄位前面的 ① ② ③ 代表它來自哪個檔案　·　右邊由上往下就是顯示順序 |
| 4-7 | 步驟 3：確認查詢畫面　／　提示：拖欄位時會即時跟著變 |

### 4B 檔案卡片（上排三張）

| # | 目前的字 |
|---|---|
| 4-8 | 卡片標籤：① 檔案1 |
| 4-9 | 空的時候：把 Excel 拖進來 ／ 或點「選擇檔案」 |
| 4-10 | 按鈕：選擇檔案… |
| 4-11 | 放進檔案後：12,345 筆資料 |
| 4-12 | 資料量偏大時多一句：　·　資料量偏大，載入會久一點 |
| 4-13 | 按鈕：更換 ／ ✕（提示：移除這個檔案） |
| 4-14 | 連結：欄位名稱不對？ ／ 展開後變成「收起來」 |
| 4-15 | 那個連結的提示：如果下面的「料號欄位」選單裡是日期、公司抬頭那種東西，代表欄位名稱不在第一列，點這裡調整 |
| 4-16 | 欄位標籤：工作表 ／ 欄位名稱在第 ／ 料號欄位 |
| 4-17 | 「欄位名稱在第」的提示：欄位名稱在第幾列（Excel 的列號） |

**比對狀況那一行**

| # | 目前的字 |
|---|---|
| 4-18 | 12,345 個料號　·　比對基準 |
| 4-19 | 與 ① 對到 8,900 個料號 |
| 4-20 | 與 ① 對不到料號，換一個料號欄位試試 |
| 4-21 | 這一欄讀不到料號 |

### 4C 欄位挑選

| # | 目前的字 |
|---|---|
| 4-22 | 左欄標題：不顯示　／　查詢時看不到這些 |
| 4-23 | 有打字篩選時：符合 12 / 87 個欄位 |
| 4-24 | 搜尋框提示字：搜尋欄位… |
| 4-25 | 左欄空的時候：檔案裡的欄位<br>全都拉到右邊了 |
| 4-26 | 右欄標題：要顯示　／　查詢時看得到，最上面那個會放大 |
| 4-27 | 右欄空的時候：把左邊的欄位拖過來<br>這裡放的就是查詢時會看到的 |
| 4-28 | 中間按鈕提示：改成查詢時要顯示 ／ 改成查詢時不顯示 |

### 4D 預覽區

| # | 目前的字 |
|---|---|
| 4-29 | 按 ⌥⇧Space 查詢時，實際會看到的畫面 |
| 4-30 | 有範例料號時：拿「C0603X7R」當範例，按 ⌥⇧Space 查詢時就長這樣 |
| 4-31 | 放進檔案後，這裡會顯示查詢結果長什麼樣子 |
| 4-32 | 把欄位拖到「要顯示」那一欄，這裡就會即時跟著變 |
| 4-33 | 這個檔案讀不到料號 |

### 4E 底部

| # | 目前的字 |
|---|---|
| 4-34 | 先放至少一個檔案進來 |
| 4-35 | 還沒選要顯示什麼 —— 把欄位拖到右邊的「要顯示」 |
| 4-36 | 料號欄位：① 料號、② PN　·　查詢時會顯示 5 個欄位 |
| 4-37 | 按鈕：取消 ／ 儲存並套用 |

### 4F 匯入時跳出來的對話框

| # | 標題 | 內容 |
|---|---|---|
| 4-38 | 讀取失敗 | 無法開啟這個檔案：（原因） |
| 4-39 | 位置滿了 | 最多同時放 3 個檔案。<br>先按某張卡片右上角的 ✕ 或「更換」再試一次。 |
| 4-40 | 尚未設定 | 請確認檔案放好了，而且右邊「要顯示」至少有一個欄位 |
| 4-41 | 選檔案視窗標題 | 選擇檔案1　／　篩選器：Excel / CSV (\*.xlsx \*.xlsm \*.xls \*.csv);;所有檔案 (\*) |
| 4-42 | **匯入完成** | ① A.xlsx　12,345 筆（料號欄位：料號）<br>② B.xlsx　8,900 筆（料號欄位：PN）<br><br>查詢時會顯示：成本、庫存、交期<br><br>注意：有料號重複出現（① 120 個料號共 260 筆）。<br>查詢時預設顯示最後一筆，畫面上會標出總共幾筆。 |

---

## 畫面 5 ｜ 權限說明（macOS）

`main.py:297-317`

| # | 目前的字 |
|---|---|
| 5-1 | 視窗標題：需要「輔助使用」權限 |
| 5-2 | 主文：⌥⇧Space（option + shift + 空白鍵）熱鍵現在不會有反應 |
| 5-3 | 說明：請到 系統設定 → 隱私權與安全性 → 輔助使用，把「終端機」打開。 |
| 5-4 | （macOS 的權限是記在「啟動這個程式的那個 App」身上，不是記在程式本身，所以這次要開的是「終端機」。換一種方式啟動就得再給一次。） |
| 5-5 | 打開之後不用重開，程式會自己接上。 |
| 5-6 | 在那之前，點選單列的黑色晶片圖示 → 查詢，一樣可以用。 |
| 5-7 | 按鈕：OK ／ 打開系統設定 |

---

## 畫面 6 ｜ 熱鍵沒掛上（Windows 主要會看到）

`main.py:259-283`

| # | 目前的字 |
|---|---|
| 6-1 | 視窗標題：熱鍵沒掛上 |
| 6-2 | 主文：Alt+Shift+Space 現在不會有反應 |
| 6-3 | 原因：（原因）／ 不明 |
| 6-4 | 在那之前，點工作列圖示 → 查詢，功能完全一樣。 |
| 6-5 | 要換一組熱鍵的話，關掉程式，打開 config/settings.json，改這兩行：<br>　　"key": "space"　　（可填 space / f2 / q …）<br>　　"modifiers": ["alt"]　（ctrl / alt / shift / cmd） |
| 6-6 | 存檔後重開程式。 |
| 6-7 | 詳細情況可以從選單的「診斷資訊…」複製給對方看。 |
| 6-8 | 按鈕：OK ／ 看診斷資訊 |

---

## 畫面 7 ｜ 已經在執行了

`main.py:128-135`

| # | 目前的字 |
|---|---|
| 7-1 | 視窗標題：PN Anywhere 已經在執行了 |
| 7-2 | 看一下螢幕右上角的選單列，應該有一顆晶片圖示。 |
| 7-3 | 如果找不到，可能是第 49731 號通訊埠被別的軟體佔走了（程式用它來避免自己被開兩次）。 |
| 7-4 | 這種情況請改設定檔裡的 app.lock_port，換一個 49000～65000 的號碼：<br>/Users/…/config/settings.json |

---

## 畫面 8 ｜ 載入有問題

`main.py:202-206`

| # | 目前的字 |
|---|---|
| 8-1 | 視窗標題：載入 Excel 資料時有問題 |
| 8-2 | （各檔案的錯誤訊息）<br><br>可以從選單列重新匯入。 |

---

## 畫面 9 ｜ 程式發生錯誤（當掉時）

`main.py:54-58`

| # | 目前的字 |
|---|---|
| 9-1 | 視窗標題：程式發生錯誤 |
| 9-2 | ValueError: xxx<br><br>詳細內容已寫進：<br>/Users/…/PN Anywhere.log<br><br>可以從工作列圖示的「診斷資訊…」複製完整報告。 |

---

## 畫面 10 ｜ 診斷資訊

`ui/diag_dialog.py`、`core/diagnostics.py`

| # | 目前的字 |
|---|---|
| 10-1 | 視窗標題：診斷資訊 |
| 10-2 | 說明：下面這段是程式的自我檢查結果。有問題的話，按「複製全部」再貼給對方，比描述症狀有用得多。 |
| 10-3 | 按鈕：存成檔案 ／ 複製全部 ／ 關閉 |
| 10-4 | 按下之後：已複製到剪貼簿 ✓ ／ 已存到 …-診斷.txt ／ 存檔失敗：… |

**報告本體的段落標題**

| # | 目前的字 |
|---|---|
| 10-5 | PN Anywhere　診斷報告 |
| 10-6 | 產生時間　2026-08-12 14:30:00 |
| 10-7 | 【執行環境】作業系統 / 系統版本 / 機器架構 / Python / 打包狀態 / 程式位置 / 設定檔位置 |
| 10-8 | 【套件】 |
| 10-9 | 【熱鍵】設定 / 這個平台的鍵碼 / 後端可用 / 目前狀態 / 解法 |
| 10-10 | 目前狀態：已掛上 ✓　／　沒掛上 ⚠　（原因） |
| 10-11 | 解法：改 config/settings.json 的 hotkey.key / hotkey.modifiers |
| 10-12 | 【權限】輔助使用權限（已授權／未授權 ⚠）/ 權限記在誰身上 |
| 10-13 | 【資料】來源檔數量 / 總筆數 / 載入耗時 / 設定檔來源數 |
| 10-14 | （還沒匯入任何檔案） |
| 10-15 | 查詢測試：用「XXXX」查 → 命中，6 筆，0.152 ms |
| 10-16 | 【設定】 |
| 10-17 | 【最近 40 行 log】 |
| 10-18 | 把整段複製起來就可以拿去問了。 |

---

## 畫面 11 ｜ log 檔裡的字（使用者通常看不到，但診斷會貼出來）

`main.py`

| # | 目前的字 |
|---|---|
| 11-1 | === 啟動（macOS）=== |
| 11-2 | 熱鍵 ⌥⇧Space：已掛上　／　掛不上 —— （原因） |
| 11-3 | 熱鍵後端不可用：（原因） |
| 11-4 | 沒有輔助使用權限，熱鍵不會有反應 |
| 11-5 | 綁不到 49731 埠，這次不啟動。 |
| 11-6 | 已載入 2 個檔案、21,245 筆資料（1.3 秒） |
| 11-7 | 載入問題：（原因） |
| 11-8 | 已啟動，常駐選單列。按 ⌥⇧Space 查詢。 |
| 11-9 | 第一次啟動，顯示引導視窗。 |
| 11-10 | === 未處理的錯誤 === |

---

## 畫面 12 ｜ 設定快捷鍵（新增）

`ui/hotkey_dialog.py`。選單列圖示 →「設定快捷鍵…」，第一次啟動的引導視窗也進得來。

| # | 目前的字 |
|---|---|
| 12-1 | 視窗標題：設定快捷鍵 |
| 12-2 | 大標：設定快捷鍵 |
| 12-3 | 副標：在任何軟體裡按這一組，就會叫出查詢視窗。 |
| 12-4 | 大方框：⌥⇧Space（按什麼顯示什麼） |
| 12-5 | 大方框底下：option + shift + 空白鍵 |
| 12-6 | 常用組合 |
| 12-7 | 四顆按鈕：⌥⇧Space／預設　·　⌥Space／兩顆鍵　·　⌃⇧Space　·　⌥⇧Q |
| 12-8 | 按鈕：取消 ／ 自己按一組… ／ 試按看看 ／ **儲存熱鍵** |

**狀態列的每一句**

| # | 什麼時候 | 目前的字 |
|---|---|---|
| 12-9 | 平常 | 按「試按看看」確認這組真的能用 |
| 12-10 | 已經試按成功過 | 已經確認可以用 ✓ |
| 12-11 | 沒有修飾鍵 | 至少要搭配一個修飾鍵（⌘ ⌥ ⌃ ⇧） |
| 12-12 | 按鍵不支援 | 這個按鍵不支援，換一個試試 |
| 12-13 | 選到 ⌘Space | 這是 Spotlight 的快捷鍵，八成會搶不到 —— 建議換一組 |
| 12-14 | 選到 ⌃Space | 這是切換輸入法的快捷鍵，八成會搶不到 —— 建議換一組 |
| 12-15 | 選到 ⌘Tab | 這是切換 App 的快捷鍵，八成會搶不到 —— 建議換一組 |
| 12-16 | Windows：Alt+Space | 這是 Windows 的視窗選單快捷鍵，八成會搶不到 —— 建議換一組 |
| 12-17 | 開始錄鍵 | 同時按住修飾鍵和主鍵，例如 option + shift + 空白鍵 |
| 12-18 | 錄鍵時大方框 | 請按下你要的組合… |
| 12-19 | 試按倒數中 | 現在按一次 ⌥⇧Space　（10 秒） |
| 12-20 | 掛不上 | 這組掛不上 —— （原因）<br>Windows 的原因會是「這組快捷鍵已經被其他程式用走了（換一組）」 |
| 12-21 | 10 秒沒按到 | 等了 10 秒沒收到 ⌥⇧Space —— 可能被別的程式搶走了，換一組試試。 |

---

## 畫面 6 的改動

6-5「打開 config/settings.json 改這兩行」整段換成：

> 多半是被別的常駐程式先搶走了。按下面的「換一組快捷鍵」，可以當場試按確認新的那組能不能用，不用重開程式。

按鈕也多一顆：**OK ／ 換一組快捷鍵 ／ 看診斷資訊**

---

## 已改（2026-08-12）

- **4-1** 視窗標題 → 匯入 Excel 資料
- **4-2** 大標 → PN Anywhere
- **5-6** 橘色 P 圖示 → 黑色晶片圖示
- **10-5** 料號查詢小工具 → PN Anywhere
- **熱鍵拼出來**：1-11、5-2、6-2 三處說明性的畫面會顯示「option + shift + 空白鍵」
- **順手一致化**：其他 6 處的「Price Book」也換掉了（2-5、2-12、3-4、3-5、3-8、8-1）——
  4-1 改成中文，選單卻還寫 Price Book 會對不起來。要留原樣跟我說。

**v1.2 新增**：設定快捷鍵（畫面 12）。現在沒有任何一個畫面會叫使用者去編輯 JSON。
